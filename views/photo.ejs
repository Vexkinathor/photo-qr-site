<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= photo.name %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="wrap photo-view">
    <h1><%= photo.name %></h1>
    <img src="/img/<%= photo.id %>" alt="<%= photo.name %>" />
  </div>
</body>
</html>
